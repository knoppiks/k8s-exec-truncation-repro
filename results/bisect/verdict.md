streaming server: the runtime's CRI streaming server truncates with no kubelet, apiserver or tunnel in the path (rung 1 failed 5/5, docker exec alone clean 5 times; rung 3 failed 6, rung 4 failed 6). The streaming server containerd runs is Kubernetes' k8s.io/cri-streaming, so this is still filed against kubernetes/kubernetes, not containerd.
